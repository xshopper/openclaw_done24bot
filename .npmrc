# done24bot npm configuration
# Skip Chromium download - we use remote done24bot service

# Puppeteer: Don't download Chromium
puppeteer_skip_download=true

# Optional: Set Puppeteer home (where browser artifacts are cached)
# puppeteer_cache_dir=/tmp/.puppeteer-cache
